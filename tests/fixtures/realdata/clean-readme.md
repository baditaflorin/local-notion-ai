# Field Ops Notes

Field Ops helps teams turn messy client notes into structured follow-up.

## What it does

- Imports markdown, HTML, CSV, and email exports.
- Extracts the most useful local summary without a server.
- Preserves local-only storage in the browser.

## Usage

Run the app locally, import a file, and review the inferred structure before sharing the output.
