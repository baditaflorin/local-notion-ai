# Contributing

Thanks for helping improve `local-notion-ai`.

## Local workflow

```sh
make install-hooks
make dev
make test
make build
make smoke
```

Use Conventional Commits for commit messages, for example `feat: add local qa workflow`.

Never commit secrets, real `.env` files, private keys, or credentials.
